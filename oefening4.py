def countTargetPairs(lijst):
    aantalParen = 0
    target = input(int())
    for i in range (len(lijst)):
        0 <= i < n 
        for j in range (len(lijst)):
             0 <= j < n and i != j
             if nums[i] + nums[j] < target:
                aantalParen =+ 1
    return aantalParen



